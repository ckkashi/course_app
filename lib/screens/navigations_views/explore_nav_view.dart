import 'package:flutter/material.dart';

class ExploreNavView extends StatelessWidget {
  const ExploreNavView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Explore Nav View'),
    );
  }
}
