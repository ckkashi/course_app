import 'package:flutter/material.dart';

class LearnNavView extends StatelessWidget {
  const LearnNavView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Learn Nav View'),
    );
  }
}
